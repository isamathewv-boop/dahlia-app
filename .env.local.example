# Copy this file to .env.local (gitignored) to enable sync locally.
# Get these from your Supabase project: Project Settings -> API.
# Leaving them unset is fine — the app works exactly as before, just without
# the "Sync across devices" section in Settings.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
